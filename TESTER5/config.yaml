server:
  port: 8080
  shutdown_timeout_seconds: 10

default_currency: USD

database:
  host: localhost
  port: 5432
  user: postgres
  password: "secret" # must be non‑empty to use PostgreSQL
  name: ecommerce
